# 运维工作平台 PRD (Product Requirements Document)

> **版本**: v1.0
> **状态**: 已确认，作为开发依据
> **目标**: 构建一个仅供单人使用的本地运维工作平台，实现日常运维工作的自动化管理

---

## 1. 项目概述

### 1.1. 背景与目标
为个人运维工作提供一个集中化、自动化的本地管理平台，解决日常工作中服务器状态监控分散、数据库巡检繁琐、定时作业管理混乱、SQL 笔记难找等痛点。通过将核心运维工作流（监控、调度、备份、文档）集成至一个本地 Web 应用中，提高工作效率。

### 1.2. 核心定位
*   **用户**: 单一用户（平台使用者本人）
*   **部署**: 仅在个人电脑本地运行
*   **网络**: 主要操作在本地浏览器，采集数据需连接内网服务器/数据库

### 1.3. 关键约束 (Must Do)
*   **持久化**: 所有数据（配置、状态、笔记、历史）必须持久化保存在本地，刷新或重启后**不可丢失**。
*   **无前端登录**: 平台在本机开放，无需账号密码登录。
*   **后端采集**: 必须包含后端服务，用于执行 SSH 连接、数据库查询、定时任务调度等无法在浏览器完成的操作。
*   **凭证管理**: 服务器 SSH 密码、数据库连接密码必须加密存储，不得明文保存在配置文件或前端可见。

### 1.4. 非目标 (暂不做)
*   移动端适配
*   云端同步
*   多用户/权限管理
*   复杂报表/BI 分析
*   邮件/短信等外部告警通知 (第一阶段仅站内提醒)

---

## 2. 功能需求 (Functional Requirements)

### 2.1. 模块总览
平台包含以下九大核心模块：

| 序号 | 模块 | 核心定位 |
| :--- | :--- | :--- |
| 1 | 运维驾驶舱 | 全局概览与告警中心 (首页) |
| 2 | 服务器资源 | 服务器状态监控与管理 |
| 3 | 数据库状态 | 数据库状态监控与管理 |
| 4 | 定时作业 | 任务调度执行与外部 Cron 管理 |
| 5 | SQL 笔记 | SQL 代码片段管理与版本控制 |
| 6 | 运维开发计划 | 运维侧任务看板 |
| 7 | 数据开发计划 | 数据侧任务看板 |
| 8 | 数据备份监控 | 数据库备份状态采集与验证 |
| 9 | 资料与设置 | 全局配置、知识库、系统信息 |

### 2.2. 模块详细需求

#### 2.2.1. 运维驾驶舱 (Cockpit)
*   **核心**: 作为默认首页，提供“一眼看全局”的视图。
*   **功能**:
    *   **统计带**: 显示服务器/数据库在线率、今日任务成功率、活跃告警数。
    *   **告警区**: 实时显示所有未处理的告警事件，支持直接跳转至源对象。
    *   **模块摘要**: 各核心模块的关键指标卡片（如：高负载服务器列表、失败作业统计）。

#### 2.2.2. 服务器资源 (Servers)
*   **核心**: 管理服务器清单并自动采集运行状态。
*   **功能**:
    *   **清单管理**: 增删改查服务器，支持标签分类。
    *   **连接配置**: 加密存储 SSH 凭证（密码/密钥）。
    *   **自动采集**: 定时通过 SSH 执行命令，采集 CPU、内存、磁盘、负载、指定进程状态等指标。
    *   **可视化**: 实时指标仪表盘 + 历史趋势图。
    *   **告警集成**: 采集异常（如 CPU 超阈值）自动生成告警。

#### 2.2.3. 数据库状态 (Databases)
*   **核心**: 管理数据库实例并监控其健康状况。
*   **支持类型**: MySQL, PostgreSQL, Oracle, DM8, Vastbase, TDSQL (等兼容协议数据库)。
*   **功能**:
    *   **清单管理**: 增删改查数据库实例，关联服务器或独立 IP。
    *   **连接配置**: 加密存储数据库账号密码。
    *   **自动采集**: 定时查询数据库，采集连接数、慢查询数、表空间、主从延迟等。
    *   **详情查看**: 实时会话列表、Top 大表、慢查询日志。
    *   **告警集成**: 会话阻塞、主从延迟过大、磁盘满等异常告警。

#### 2.2.4. 定时作业 (Jobs)
*   **核心**: 统一管理定时任务，既包括平台内执行的，也包括服务器原生的。
*   **功能**:
    *   **平台调度**: 内置 Cron 调度器，支持创建在服务器上执行的脚本任务或在数据库执行的 SQL 任务。
    *   **外部采集**: 定时通过 SSH 读取服务器的 `crontab` 内容，展示原生定时任务列表（只读）。
    *   **执行日志**: 详细记录每次任务的执行时间、耗时、状态（成功/失败）、输出结果。
    *   **告警集成**: 任务执行失败自动告警。

#### 2.2.5. SQL 笔记 (SQL Notes)
*   **核心**: 个人 SQL 代码片段库，支持版本控制。
*   **功能**:
    *   **分类管理**: 支持按数据库类型（MySQL, PG等）或自定义文件夹分类。
    *   **代码存储**: 支持语法高亮，纯文本存储。
    *   **版本控制**: 每次编辑保存生成新版本，可查看版本历史、对比差异、回滚。
    *   **全文检索**: 支持按标题、内容、标签快速搜索。
    *   **只读**: **注意**，此模块仅作笔记存储，**不提供**直接连接数据库执行 SQL 的功能。

#### 2.2.6. & 2.2.7. 开发计划 (Dev Plans - OPS / Data)
*   **核心**: 轻量级任务看板，分别用于记录运维开发任务和数据开发任务。
*   **功能**:
    *   **看板视图**: 三列布局（待办/进行中/已完成），支持拖拽变更状态。
    *   **卡片内容**: 包含任务标题、描述、优先级、负责人、截止日期、进度百分比。
    *   **列表视图**: 支持切换为表格视图，便于批量筛选和排序。

#### 2.2.8. 数据备份监控 (Backup Monitor)
*   **核心**: 监控由外部脚本/系统生成的数据库备份的健康状态。
*   **功能**:
    *   **采集验证**: 通过 SSH 检查备份目录下的文件大小、生成时间，或连接数据库查询备份记录表。
    *   **状态展示**: 清晰展示最近一次备份的状态（成功/失败/未找到）。
    *   **告警集成**: 备份失败、备份文件过大/过小、超过保留期限未备份均需告警。
    *   **注意**: 此模块仅“监控”和“验证”，**不主动执行**备份操作。

#### 2.2.9. 资料与设置 (Settings)
*   **核心**: 平台的基础配置中心和个人知识库。
*   **功能**:
    *   **全局设置**: 调整采集频率、告警阈值、界面主题、数据存储路径等。
    *   **资料库**: 内置 Markdown 编辑器，用于编写和存储个人运维文档、操作手册。
    *   **系统信息**: 查看平台运行日志，提供“导出/导入数据”功能作为本地方案备份。

---

## 3. 设计规范 (Design Specification)

### 3.1. 视觉风格
*   **基调**: 温暖、低饱和度、非典型“冷冰冰”的控制台风格，像一个精心整理的个人工作台。
*   **色彩系统**:
    *   **主背景**: 暖米白/奶油色 (`#F3EDE4`)
    *   **侧栏**: 深炭灰近黑 (`#2B2B2B`)
    *   **主强调色**: 橄榄绿/鼠尾草绿 (`#7A8B6F`) - 用于按钮、激活态、正常状态
    *   **警告色**: 琥珀黄 - 用于告警提示
    *   **异常色**: 铁锈红 - 用于错误状态
*   **组件风格**:
    *   **卡片**: 白底，极淡阴影，圆角。
    *   **按钮**: 绿色实心按钮为主，辅以描边线按钮。
    *   **字体**: 系统默认无衬线字体，标题加粗，正文常规。
    *   **密度**: 中等信息密度，优先使用卡片化布局，而非高密度表格墙。

### 3.2. 交互规范
*   **布局**: 全局采用“左侧导航 + 顶部工具栏 + 主内容区”的经典布局。
*   **导航**: 左侧导航栏可折叠，支持模块图标。
*   **快速新增**: 侧栏顶部提供“+ 快速新增”按钮，可快捷创建各类资源。
*   **告警联动**: 所有告警事件来源于**统一的告警表**，驾驶舱展示聚合视图，详情页展示过滤后的列表。
*   **数据持久化**: 所有交互状态（如筛选条件、排序）在当前会话中保持。

---

## 4. 数据模型概念 (Data Model Concepts)

虽然具体表结构待技术设计阶段确定，但需提前定义核心实体：

*   **User (隐含)**: 单用户模式，不建表。
*   **Server**: id, name, ip, os_type, ssh_credentials (encrypted), ...
*   **Database**: id, name, type, host, db_credentials (encrypted), server_id (nullable), ...
*   **Metric (Server/DB)**: id, target_id, target_type, metric_name, value, timestamp, ...
*   **Job (Platform)**: id, name, target_id, schedule (cron), script/sql, status, ...
*   **JobRun (History)**: id, job_id, start_time, end_time, status, output, ...
*   **Alert**: id, source_type, source_id, level, type, message, status (unresolved/resolved), ...
*   **SQLNote**: id, title, content, db_type, folder, tags, version, ...
*   **PlanTask**: id, title, description, status, priority, due_date, progress, assignee, plan_type (ops/data), ...

---

## 5. 下一步行动

本 PRD 确认后，将进入**技术选型与项目初始化**阶段，重点确定：
1.  **技术栈**: 前端框架 (Vue/React)、后端语言 (Python Go Node)、数据库 (SQLite)。
2.  **项目骨架**: 搭建前后端分离的项目结构。
3.  **核心依赖**: 集成 SSH 客户端库、数据库连接池、调度器库等。